<UserControl x:Class="SelectorLineasPlantillaVenta"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:controlesusuario="clr-namespace:ControlesUsuario;assembly=ControlesUsuario"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="SelectorControl"
             d:DesignHeight="400" d:DesignWidth="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra de filtro -->
        <controlesusuario:BarraFiltro
            x:Name="BarraFiltroInternal"
            Grid.Row="0"
            ListaItems="{Binding ListaFiltrable, ElementName=SelectorControl}"
            Visibility="{Binding MostrarBarraFiltroVisibility, ElementName=SelectorControl}"/>

        <!-- Lista de productos -->
        <ListView x:Name="InternalListView"
                  Grid.Row="1"
                  ItemsSource="{Binding ListaFiltrable.Lista, ElementName=SelectorControl}"
                  SelectedItem="{Binding SelectedItem, ElementName=SelectorControl, Mode=TwoWay}"
                  HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Stretch"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <DockPanel Height="110" Margin="6" HorizontalAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Hidden">
                        <StackPanel DockPanel.Dock="Left" Grid.Column="0" Width="110" MinWidth="110" Height="110" >
                            <Image Source="{Binding imagen}" Height="100" Width="100" Visibility="{Binding imagenVisible}" Margin="0,0,10,0">
                                <xctk:MagnifierManager.Magnifier>
                                    <xctk:Magnifier Radius="100" ZoomFactor=".3" />
                                </xctk:MagnifierManager.Magnifier>
                            </Image>
                            <Border Background="{Binding colorEstado}" Height="110" Width="110" HorizontalAlignment="Left" Margin="0,0,10,0">
                                <TextBlock Text="{Binding clasificacionMasVendidos}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontWeight="Bold" FontSize="24" Visibility="{Binding clasificacionVisible}"></TextBlock>
                            </Border>
                        </StackPanel>

                        <DockPanel DockPanel.Dock="Right" HorizontalAlignment="Stretch">
                            <Grid DockPanel.Dock="Right" Width="110" MinWidth="110" Height="110" HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Margin="3" TextWrapping="WrapWithOverflow" Text="Cantidad Cobrada"></TextBlock>
                                    <xctk:ShortUpDown Value="{Binding cantidad}" Margin="3">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="ValueChanged">
                                                <i:InvokeCommandAction Command="{Binding ActualizarProductoCommand, ElementName=SelectorControl}" CommandParameter="{Binding}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </xctk:ShortUpDown>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Visibility="{Binding PermitirCantidadOfertaVisibility, ElementName=SelectorControl}">
                                    <TextBlock Margin="3" TextWrapping="WrapWithOverflow" >Cantidad Oferta</TextBlock>
                                    <xctk:ShortUpDown Value="{Binding cantidadOferta}" Margin="3" IsEnabled="{Binding aplicarDescuentoFicha}" >
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="ValueChanged">
                                                <i:InvokeCommandAction Command="{Binding ActualizarProductoCommand, ElementName=SelectorControl}" CommandParameter="{Binding}"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </xctk:ShortUpDown>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding textoUnidadesDisponibles}" Foreground="{Binding colorStock}"/>
                                    <TextBlock Text="{Binding textoStocksPorAlmacen}" FontSize="10" Foreground="Gray" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Grid>

                            <DockPanel DockPanel.Dock="Right" HorizontalAlignment="Stretch">
                                <StackPanel DockPanel.Dock="Left" VerticalAlignment="Top" Margin="10,0,0,0" HorizontalAlignment="Stretch">
                                    <TextBlock Text="{Binding textoNombreProducto}" FontWeight="Bold" TextWrapping="WrapWithOverflow" HorizontalAlignment="Stretch"/>
                                    <TextBlock Text="{Binding producto}" TextWrapping="WrapWithOverflow" HorizontalAlignment="Stretch"/>
                                    <TextBlock Text="{Binding familia}" MaxHeight="60" TextWrapping="WrapWithOverflow" HorizontalAlignment="Stretch"/>
                                    <TextBlock Text="{Binding subGrupo}" MaxHeight="60" TextWrapping="WrapWithOverflow" HorizontalAlignment="Stretch"/>
                                    <TextBlock Text="{Binding precio, StringFormat=c}" MaxHeight="60" TextWrapping="WrapWithOverflow" HorizontalAlignment="Stretch" Visibility="{Binding MostrarPrecioVisibility, ElementName=SelectorControl}"/>
                                    <TextBlock Text="{Binding textoFechaUltimaVenta}" MaxHeight="60" TextWrapping="WrapWithOverflow" HorizontalAlignment="Stretch"/>
                                    <TextBlock Text="{Binding textoUnidadesVendidas}" TextWrapping="WrapWithOverflow" HorizontalAlignment="Stretch"/>
                                </StackPanel>
                            </DockPanel>
                        </DockPanel>
                    </DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
