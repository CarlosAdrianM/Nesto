<UserControl x:Class="SelectorGrupoComisionDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="420">
    <!-- NestoAPI#352: una línea de inmovilizado comisiona por el grupo que elige quien mete
         el pedido. Se pregunta al guardar, una vez por línea sin grupo. -->
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Mensaje}" TextWrapping="Wrap" Margin="0,0,0,10" />

        <ComboBox Grid.Row="1" ItemsSource="{Binding Grupos}"
                  SelectedValue="{Binding GrupoSeleccionado, Mode=TwoWay}"
                  SelectedValuePath="Codigo"
                  DisplayMemberPath="TextoFormateado"
                  Width="260" HorizontalAlignment="Left" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Aceptar" Command="{Binding AceptarCommand}" Width="90" Height="28" Margin="0,0,8,0" IsDefault="True" />
            <Button Content="Cancelar" Command="{Binding CancelarCommand}" Width="90" Height="28" IsCancel="True" />
        </StackPanel>
    </Grid>
</UserControl>
