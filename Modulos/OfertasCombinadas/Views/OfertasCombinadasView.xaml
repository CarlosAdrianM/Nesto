<UserControl x:Class="Nesto.Modulos.OfertasCombinadas.Views.OfertasCombinadasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Nesto.Modulos.OfertasCombinadas.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:ControlesUsuario.Behaviors;assembly=ControlesUsuario"
             xmlns:controles="clr-namespace:ControlesUsuario;assembly=ControlesUsuario"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <controles:BindingProxy x:Key="ViewModelProxy" Data="{Binding}" />
    </UserControl.Resources>

    <xctk:BusyIndicator IsBusy="{Binding EstaCargando}" BusyContent="Cargando...">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Barra superior -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <Button Command="{Binding CargarCommand}"
                        Padding="10,5" Margin="0,0,10,0">
                    Refrescar
                </Button>
                <CheckBox Content="Solo activas"
                          IsChecked="{Binding SoloActivas}"
                          VerticalAlignment="Center"
                          Margin="10,0,0,0"
                          ToolTip="Mostrar solo ofertas vigentes (FechaDesde &lt;= hoy y FechaHasta nula o >= hoy)"/>
                <TextBlock Text="Mantenimiento de Ofertas Combinadas"
                           VerticalAlignment="Center" FontWeight="Bold" Margin="20,0,0,0"/>
            </StackPanel>

            <!-- TabControl con dos secciones -->
            <TabControl Grid.Row="1">

                <!-- Tab 1: Ofertas Combinadas (master/detail) -->
                <TabItem Header="Ofertas Combinadas">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="200"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Boton nueva oferta -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                            <Button Command="{Binding NuevaOfertaCombinadaCommand}"
                                    Padding="10,5" Margin="0,0,10,0">
                                Nueva oferta
                            </Button>
                        </StackPanel>

                        <!-- DataGrid maestro: ofertas combinadas -->
                        <DataGrid x:Name="dgOfertasCombinadas"
                                  Grid.Row="1"
                                  ItemsSource="{Binding OfertasCombinadas}"
                                  SelectedItem="{Binding OfertaCombinadaSeleccionada}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  SelectionMode="Single"
                                  IsReadOnly="False"
                                  RowHeight="28"
                                  GridLinesVisibility="Horizontal"
                                  AlternatingRowBackground="#F9F9F9">
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="DataGridCell_PreviewMouseLeftButtonDown"/>
                                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns>
                                <!-- Nombre -->
                                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre, UpdateSourceTrigger=LostFocus}" Width="350">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Importe Minimo -->
                                <DataGridTextColumn Header="Importe Min." Binding="{Binding ImporteMinimo, UpdateSourceTrigger=LostFocus, StringFormat=N2}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Margin" Value="2,0,5,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                    <DataGridTextColumn.EditingElementStyle>
                                        <Style TargetType="TextBox">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.EditingElementStyle>
                                </DataGridTextColumn>

                                <!-- Fecha Desde -->
                                <DataGridTemplateColumn Header="Fecha Desde" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FechaDesde, StringFormat=d}" VerticalAlignment="Center" Margin="2,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <DatePicker SelectedDate="{Binding FechaDesde, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <!-- Fecha Hasta -->
                                <DataGridTemplateColumn Header="Fecha Hasta" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FechaHasta, StringFormat=d}" VerticalAlignment="Center" Margin="2,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <DatePicker SelectedDate="{Binding FechaHasta, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <!-- NestoAPI#290: regalo = menor importe (por defecto marcado) -->
                                <DataGridCheckBoxColumn Header="Regalo menor imp." Binding="{Binding RegalarMenorImporte, UpdateSourceTrigger=PropertyChanged}" Width="110">
                                    <DataGridCheckBoxColumn.ElementStyle>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="ToolTip" Value="La unidad que va gratis debe ser la de menor tarifa del conjunto, y las pagadas se cobran a su tarifa (el importe mínimo se calcula solo por combinación). Desmarcar en promos que regalan a propósito un artículo más caro que lo comprado."/>
                                        </Style>
                                    </DataGridCheckBoxColumn.ElementStyle>
                                    <DataGridCheckBoxColumn.EditingElementStyle>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridCheckBoxColumn.EditingElementStyle>
                                </DataGridCheckBoxColumn>

                                <!-- NestoAPI#292: el usuario piensa en "3+2". Cobradas ajusta sola la
                                     cantidad de la fila (config de un grupo o una fila); Regaladas se
                                     guarda en la oferta. Se cobran las más caras a tarifa y se regalan
                                     las más baratas. -->
                                <!-- Sin "Regalo menor imp." estos dos campos no aplican (el servidor los
                                     ignora y UnidadesRegaladas tiene mínimo 1): se ocultan para que una
                                     oferta sin regalo no se lea como un falso "13+1". -->
                                <DataGridTextColumn Header="Cobradas" Binding="{Binding UnidadesCobradas, UpdateSourceTrigger=PropertyChanged}" Width="65">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="ToolTip" Value="Unidades que se cobran (a su tarifa) por cada oferta completa. En un 3+2 ponga 3: la cantidad de la fila se ajusta sola a 5."/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RegalarMenorImporte}" Value="False">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Regaladas" Binding="{Binding UnidadesRegaladas, UpdateSourceTrigger=PropertyChanged}" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="ToolTip" Value="Unidades que salen gratis por cada oferta completa (las de menor tarifa del conjunto). En un 3+2 ponga 2. Requiere 'Regalo menor imp.' para más de una."/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RegalarMenorImporte}" Value="False">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Oferta" Binding="{Binding ResumenOferta}" IsReadOnly="True" Width="55">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="ToolTip" Value="Resumen cobradas+regaladas de la oferta."/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Acciones -->
                                <DataGridTemplateColumn Header="Acciones" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Content="Guardar"
                                                        Command="{Binding Data.GuardarOfertaCombinadaCommand, Source={StaticResource ViewModelProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Margin="2" Padding="5,2"
                                                        VerticalAlignment="Center"
                                                        Visibility="{Binding HaCambiado, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                <Button Content="Eliminar"
                                                        Command="{Binding Data.EliminarOfertaCombinadaCommand, Source={StaticResource ViewModelProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Margin="2" Padding="5,2"
                                                        VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Usuario -->
                                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="150" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Fecha Modificacion -->
                                <DataGridTextColumn Header="Modificado" Binding="{Binding FechaModificacion, StringFormat=g}" Width="130" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Separador y cabecera detalle -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,5">
                            <TextBlock Text="Productos de la oferta seleccionada" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Button Command="{Binding NuevoDetalleCommand}"
                                    Padding="10,3" Margin="15,0,0,0">
                                Nuevo producto
                            </Button>
                            <Button Command="{Binding NuevoDetalleAlternativoCommand}"
                                    Padding="10,3" Margin="10,0,0,0"
                                    ToolTip="Añade una alternativa intercambiable a la línea seleccionada (mismo grupo, p. ej. otra talla de la camiseta). El pedido podrá llevar una cualquiera de ellas.">
                                Producto alternativo
                            </Button>
                        </StackPanel>

                        <Separator Grid.Row="3"/>

                        <!-- DataGrid detalle: productos de la oferta -->
                        <DataGrid x:Name="dgDetalles"
                                  Grid.Row="4"
                                  ItemsSource="{Binding DetallesOfertaSeleccionada}"
                                  SelectedItem="{Binding DetalleSeleccionado}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  SelectionMode="Single"
                                  IsReadOnly="False"
                                  RowHeight="28"
                                  GridLinesVisibility="Horizontal"
                                  AlternatingRowBackground="#F0F8FF">
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="DataGridCell_PreviewMouseLeftButtonDown"/>
                                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <!-- Producto con Autocomplete -->
                                <DataGridTemplateColumn Header="Producto" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Producto}" VerticalAlignment="Center" Margin="2,0" FontWeight="SemiBold"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Producto, UpdateSourceTrigger=LostFocus}"
                                                     VerticalAlignment="Center"
                                                     VerticalContentAlignment="Center">
                                                <i:Interaction.Behaviors>
                                                    <behaviors:AutocompleteBehavior
                                                        Empresa="{Binding Data.Empresa, Source={StaticResource ViewModelProxy}}"
                                                        TipoBusqueda="Producto"
                                                        SiempreActivo="True"
                                                        MinChars="2"
                                                        DebounceMs="300"
                                                        MaxResults="10"/>
                                                    <behaviors:ProductoBehavior
                                                        Empresa="{Binding Data.Empresa, Source={StaticResource ViewModelProxy}}"
                                                        SiempreActivo="True"
                                                        Texto="{Binding DataContext.texto, RelativeSource={RelativeSource AncestorType=DataGridRow}, Mode=TwoWay}"/>
                                                </i:Interaction.Behaviors>
                                            </TextBox>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <!-- Nombre producto (solo lectura) -->
                                <DataGridTextColumn Header="Nombre" Binding="{Binding ProductoNombre}" Width="220" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Familia (línea de filtro: producto vacío) -->
                                <DataGridTextColumn Header="Familia" Binding="{Binding Familia, UpdateSourceTrigger=LostFocus}" Width="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                            <Setter Property="ToolTip" Value="Solo para líneas de filtro (sin producto): casa los productos de esta familia."/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Filtro por principio del nombre (línea de filtro: producto vacío) -->
                                <DataGridTextColumn Header="Filtro (nombre empieza por)" Binding="{Binding FiltroProducto, UpdateSourceTrigger=LostFocus}" Width="170">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                            <Setter Property="ToolTip" Value="Solo para líneas de filtro (sin producto): casa los productos cuyo nombre empiece por este texto."/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Subgrupo (línea de filtro, NestoAPI#289): combo ordenado por grupo y
                                     descripción; la opción en blanco deja la fila sin filtro de subgrupo
                                     (funciona exactamente igual que antes). -->
                                <DataGridTemplateColumn Header="Subgrupo" Width="230">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding Data.Subgrupos, Source={StaticResource ViewModelProxy}}"
                                                      DisplayMemberPath="Etiqueta"
                                                      SelectedValuePath="Clave"
                                                      SelectedValue="{Binding GrupoSubgrupoClave, Mode=TwoWay}"
                                                      VerticalAlignment="Center"
                                                      ToolTip="Solo para líneas de filtro (sin producto): casa los productos de este subgrupo."/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Cantidad -->
                                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad, UpdateSourceTrigger=LostFocus}" Width="80">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Margin" Value="2,0,5,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                    <DataGridTextColumn.EditingElementStyle>
                                        <Style TargetType="TextBox">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.EditingElementStyle>
                                </DataGridTextColumn>

                                <!-- Precio -->
                                <DataGridTextColumn Header="Precio" Binding="{Binding Precio, UpdateSourceTrigger=LostFocus, StringFormat=N2}" Width="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Margin" Value="2,0,5,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                    <DataGridTextColumn.EditingElementStyle>
                                        <Style TargetType="TextBox">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.EditingElementStyle>
                                </DataGridTextColumn>

                                <!-- Grupo de alternativas: líneas con el mismo número son intercambiables ("elige 1") -->
                                <DataGridTextColumn Header="Grupo alt." Binding="{Binding GrupoAlternativa, UpdateSourceTrigger=LostFocus}" Width="80"
                                                    ClipboardContentBinding="{x:Null}">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                    <DataGridTextColumn.EditingElementStyle>
                                        <Style TargetType="TextBox">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.EditingElementStyle>
                                </DataGridTextColumn>

                                <!-- Permitir cantidad menor: la Cantidad pasa a ser un máximo (0..Cantidad) -->
                                <DataGridCheckBoxColumn Header="Cant. menor" Binding="{Binding PermitirCantidadMenor, UpdateSourceTrigger=PropertyChanged}" Width="90">
                                    <DataGridCheckBoxColumn.ElementStyle>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="ToolTip" Value="Si se marca, la Cantidad es un máximo: el pedido puede llevar de 0 a esa cantidad de este producto (p. ej. folletos o expositor) sin que la oferta deje de validar."/>
                                        </Style>
                                    </DataGridCheckBoxColumn.ElementStyle>
                                    <DataGridCheckBoxColumn.EditingElementStyle>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridCheckBoxColumn.EditingElementStyle>
                                </DataGridCheckBoxColumn>

                                <!-- Eliminar detalle -->
                                <DataGridTemplateColumn Header="" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Quitar"
                                                    Command="{Binding Data.EliminarDetalleCommand, Source={StaticResource ViewModelProxy}}"
                                                    CommandParameter="{Binding}"
                                                    Margin="2" Padding="5,2"
                                                    VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Info inferior -->
                        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="Total ofertas: " VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding OfertasCombinadas.Count}" VerticalAlignment="Center" FontWeight="Bold"/>
                            <TextBlock Text=" | Precio=0 significa sin restriccion de precio. Cantidad=0 significa producto opcional. Mismo 'Grupo alt.' = alternativas intercambiables (se exige 1 cualquiera). 'Cant. menor' = la cantidad es un máximo (se admite de 0 a esa cantidad). Línea de FILTRO: deje el producto vacío y rellene familia y/o filtro; la cantidad se suma entre todos los productos que casan (no admite grupo alt.)."
                                       VerticalAlignment="Center" Margin="20,0,0,0"
                                       Foreground="Gray" FontStyle="Italic"
                                       TextWrapping="Wrap" MaxWidth="900"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <!-- Tab 2: Ofertas por Familia -->
                <TabItem Header="Ofertas por Familia">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Boton nueva oferta familia -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                            <Button Command="{Binding NuevaOfertaFamiliaCommand}"
                                    Padding="10,5" Margin="0,0,10,0">
                                Nueva oferta familia
                            </Button>
                        </StackPanel>

                        <!-- DataGrid ofertas por familia -->
                        <DataGrid x:Name="dgOfertasFamilia"
                                  Grid.Row="1"
                                  ItemsSource="{Binding OfertasFamilia}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  SelectionMode="Single"
                                  IsReadOnly="False"
                                  RowHeight="28"
                                  GridLinesVisibility="Horizontal"
                                  AlternatingRowBackground="#F9F9F9">
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="DataGridCell_PreviewMouseLeftButtonDown"/>
                                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns>
                                <!-- Familia -->
                                <DataGridTextColumn Header="Familia" Binding="{Binding Familia, UpdateSourceTrigger=LostFocus}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Descripcion Familia (solo lectura) -->
                                <DataGridTextColumn Header="Descripcion" Binding="{Binding FamiliaDescripcion}" Width="200" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Cantidad con Precio -->
                                <DataGridTextColumn Header="Cant. con Precio" Binding="{Binding CantidadConPrecio, UpdateSourceTrigger=LostFocus}" Width="110">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Margin" Value="2,0,5,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                    <DataGridTextColumn.EditingElementStyle>
                                        <Style TargetType="TextBox">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.EditingElementStyle>
                                </DataGridTextColumn>

                                <!-- Cantidad Regalo -->
                                <DataGridTextColumn Header="Cant. Regalo" Binding="{Binding CantidadRegalo, UpdateSourceTrigger=LostFocus}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="Margin" Value="2,0,5,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                    <DataGridTextColumn.EditingElementStyle>
                                        <Style TargetType="TextBox">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.EditingElementStyle>
                                </DataGridTextColumn>

                                <!-- Filtro Producto -->
                                <DataGridTextColumn Header="Filtro Producto" Binding="{Binding FiltroProducto, UpdateSourceTrigger=LostFocus}" Width="300">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Acciones -->
                                <DataGridTemplateColumn Header="Acciones" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Content="Guardar"
                                                        Command="{Binding Data.GuardarOfertaFamiliaCommand, Source={StaticResource ViewModelProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Margin="2" Padding="5,2"
                                                        VerticalAlignment="Center"
                                                        Visibility="{Binding HaCambiado, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                <Button Content="Eliminar"
                                                        Command="{Binding Data.EliminarOfertaFamiliaCommand, Source={StaticResource ViewModelProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Margin="2" Padding="5,2"
                                                        VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Usuario -->
                                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="150" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Fecha Modificacion -->
                                <DataGridTextColumn Header="Modificado" Binding="{Binding FechaModificacion, StringFormat=g}" Width="130" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Info inferior -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="Total ofertas familia: " VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding OfertasFamilia.Count}" VerticalAlignment="Center" FontWeight="Bold"/>
                            <TextBlock Text=" | El filtro busca productos cuyo nombre empiece por el texto indicado (dentro de la familia)"
                                       VerticalAlignment="Center" Margin="20,0,0,0"
                                       Foreground="Gray" FontStyle="Italic"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <!-- Tab 3: Ofertas Escalonadas (descuento por volumen sobre lista de referencias) -->
                <TabItem Header="Ofertas Escalonadas">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="230"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Boton nueva oferta escalonada -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                            <Button Command="{Binding NuevaOfertaEscalonadaCommand}"
                                    Padding="10,5" Margin="0,0,10,0">
                                Nueva oferta escalonada
                            </Button>
                        </StackPanel>

                        <!-- DataGrid maestro: ofertas escalonadas -->
                        <DataGrid x:Name="dgOfertasEscalonadas"
                                  Grid.Row="1"
                                  ItemsSource="{Binding OfertasEscalonadas}"
                                  SelectedItem="{Binding OfertaEscalonadaSeleccionada}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  SelectionMode="Single"
                                  IsReadOnly="False"
                                  RowHeight="28"
                                  GridLinesVisibility="Horizontal"
                                  AlternatingRowBackground="#F9F9F9">
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <EventSetter Event="PreviewMouseLeftButtonDown" Handler="DataGridCell_PreviewMouseLeftButtonDown"/>
                                    <Setter Property="VerticalAlignment" Value="Stretch"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns>
                                <!-- Nombre -->
                                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre, UpdateSourceTrigger=LostFocus}" Width="350">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Fecha Desde -->
                                <DataGridTemplateColumn Header="Fecha Desde" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FechaDesde, StringFormat=d}" VerticalAlignment="Center" Margin="2,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <DatePicker SelectedDate="{Binding FechaDesde, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <!-- Fecha Hasta -->
                                <DataGridTemplateColumn Header="Fecha Hasta" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FechaHasta, StringFormat=d}" VerticalAlignment="Center" Margin="2,0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <DatePicker SelectedDate="{Binding FechaHasta, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>

                                <!-- Acciones -->
                                <DataGridTemplateColumn Header="Acciones" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Content="Guardar"
                                                        Command="{Binding Data.GuardarOfertaEscalonadaCommand, Source={StaticResource ViewModelProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Margin="2" Padding="5,2"
                                                        VerticalAlignment="Center"
                                                        Visibility="{Binding HaCambiado, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                <Button Content="Eliminar"
                                                        Command="{Binding Data.EliminarOfertaEscalonadaCommand, Source={StaticResource ViewModelProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Margin="2" Padding="5,2"
                                                        VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Usuario -->
                                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="150" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Fecha Modificacion -->
                                <DataGridTextColumn Header="Modificado" Binding="{Binding FechaModificacion, StringFormat=g}" Width="130" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="2,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Pegar referencias -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,5">
                            <TextBlock Text="Referencias:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <!-- AcceptsReturn=true para que un pegado multilínea no se trunque en el
                                 primer salto de línea; la altura es la de un TextBox normal y el
                                 contenido extra simplemente queda desplazado. -->
                            <TextBox Text="{Binding ReferenciasTexto, UpdateSourceTrigger=PropertyChanged}"
                                     Width="450" Margin="10,0,0,0"
                                     AcceptsReturn="True"
                                     VerticalAlignment="Center"
                                     VerticalContentAlignment="Center"
                                     ToolTip="Pegue aquí las referencias separadas por comas, espacios o saltos de línea (p. ej. 44707,44708,44709)"/>
                            <Button Command="{Binding AnadirReferenciasCommand}"
                                    Padding="10,3" Margin="10,0,0,0"
                                    VerticalAlignment="Center"
                                    ToolTip="Añade las referencias pegadas a la oferta seleccionada. El precio base se rellena con el PVP de ficha al guardar.">
                                Añadir referencias
                            </Button>
                            <Button Command="{Binding NuevoProductoEscalonadoCommand}"
                                    Padding="10,3" Margin="10,0,0,0"
                                    VerticalAlignment="Center">
                                Nuevo producto
                            </Button>
                            <Button Command="{Binding NuevoTramoCommand}"
                                    Padding="10,3" Margin="10,0,0,0"
                                    VerticalAlignment="Center">
                                Nuevo tramo
                            </Button>
                        </StackPanel>

                        <Separator Grid.Row="3"/>

                        <!-- Productos y tramos de la oferta seleccionada -->
                        <Grid Grid.Row="4" Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- DataGrid productos -->
                            <DataGrid x:Name="dgProductosEscalonada"
                                      Grid.Column="0"
                                      ItemsSource="{Binding OfertaEscalonadaSeleccionada.Productos}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      SelectionMode="Single"
                                      IsReadOnly="False"
                                      RowHeight="28"
                                      GridLinesVisibility="Horizontal"
                                      AlternatingRowBackground="#F0F8FF">
                                <DataGrid.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="DataGridCell_PreviewMouseLeftButtonDown"/>
                                        <Setter Property="VerticalAlignment" Value="Stretch"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGrid.CellStyle>
                                <DataGrid.Columns>
                                    <!-- Producto con Autocomplete -->
                                    <DataGridTemplateColumn Header="Producto" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Producto}" VerticalAlignment="Center" Margin="2,0" FontWeight="SemiBold"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                        <DataGridTemplateColumn.CellEditingTemplate>
                                            <DataTemplate>
                                                <TextBox Text="{Binding Producto, UpdateSourceTrigger=LostFocus}"
                                                         VerticalAlignment="Center"
                                                         VerticalContentAlignment="Center">
                                                    <i:Interaction.Behaviors>
                                                        <behaviors:AutocompleteBehavior
                                                            Empresa="{Binding Data.Empresa, Source={StaticResource ViewModelProxy}}"
                                                            TipoBusqueda="Producto"
                                                            SiempreActivo="True"
                                                            MinChars="2"
                                                            DebounceMs="300"
                                                            MaxResults="10"/>
                                                        <behaviors:ProductoBehavior
                                                            Empresa="{Binding Data.Empresa, Source={StaticResource ViewModelProxy}}"
                                                            SiempreActivo="True"
                                                            Texto="{Binding DataContext.texto, RelativeSource={RelativeSource AncestorType=DataGridRow}, Mode=TwoWay}"/>
                                                    </i:Interaction.Behaviors>
                                                </TextBox>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellEditingTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- Nombre producto (solo lectura; en rojo si la referencia no existe) -->
                                    <DataGridTextColumn Header="Nombre" Binding="{Binding ProductoNombre}" Width="*" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Margin" Value="2,0"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding NoEncontrado}" Value="True">
                                                        <Setter Property="Foreground" Value="Red"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <!-- Precio base -->
                                    <DataGridTextColumn Header="Precio base" Binding="{Binding PrecioBase, UpdateSourceTrigger=LostFocus, StringFormat=N2, TargetNullValue=''}" Width="90">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Margin" Value="2,0,5,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>

                                    <!-- Eliminar producto -->
                                    <DataGridTemplateColumn Header="" Width="70">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Quitar"
                                                        Command="{Binding Data.EliminarProductoEscalonadoCommand, Source={StaticResource ViewModelProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Margin="2" Padding="5,2"
                                                        VerticalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>

                            <!-- DataGrid tramos -->
                            <DataGrid x:Name="dgTramosEscalonada"
                                      Grid.Column="2"
                                      ItemsSource="{Binding OfertaEscalonadaSeleccionada.Tramos}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      SelectionMode="Single"
                                      IsReadOnly="False"
                                      RowHeight="28"
                                      GridLinesVisibility="Horizontal"
                                      AlternatingRowBackground="#FFF8F0">
                                <DataGrid.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="DataGridCell_PreviewMouseLeftButtonDown"/>
                                        <Setter Property="VerticalAlignment" Value="Stretch"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGrid.CellStyle>
                                <DataGrid.Columns>
                                    <!-- Cantidad minima -->
                                    <DataGridTextColumn Header="Cantidad (o más)" Binding="{Binding CantidadMinima, UpdateSourceTrigger=LostFocus}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Margin" Value="2,0,5,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>

                                    <!-- % Descuento -->
                                    <DataGridTextColumn Header="% Descuento" Binding="{Binding DescuentoPorcentaje, UpdateSourceTrigger=LostFocus, StringFormat=N2}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Margin" Value="2,0,5,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                        <DataGridTextColumn.EditingElementStyle>
                                            <Style TargetType="TextBox">
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                            </Style>
                                        </DataGridTextColumn.EditingElementStyle>
                                    </DataGridTextColumn>

                                    <!-- Eliminar tramo -->
                                    <DataGridTemplateColumn Header="" Width="70">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Quitar"
                                                        Command="{Binding Data.EliminarTramoCommand, Source={StaticResource ViewModelProxy}}"
                                                        CommandParameter="{Binding}"
                                                        Margin="2" Padding="5,2"
                                                        VerticalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>

                        <!-- Info inferior -->
                        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="Total ofertas escalonadas: " VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding OfertasEscalonadas.Count}" VerticalAlignment="Center" FontWeight="Bold"/>
                            <TextBlock Text=" | Las cantidades de las referencias se suman entre sí. Cada tramo es 'cantidad o más' y aplica su % sobre el precio base de cada referencia. Precio base vacío = PVP de ficha al guardar."
                                       VerticalAlignment="Center" Margin="20,0,0,0"
                                       Foreground="Gray" FontStyle="Italic"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

            </TabControl>
        </Grid>
    </xctk:BusyIndicator>
</UserControl>
